-- allium: 3
-- source-data-sync.allium
--
-- Scope: How weft moves a job's source tree and declared data inputs from the
--        user's working directory to the host (or cloud instance) that will
--        actually run the job.
-- Includes:
--   - Source snapshotting: deterministic, content-addressed tarballs of the
--     user's working directory with exclude rules for VCS, caches, build
--     artifacts, and declared output dirs.
--   - Multi-root source snapshots: declared sibling roots mounted adjacent
--     to the project with one content-addressed tarball per root and one
--     manifest hash for the complete source identity.
--   - On-prem source sync: rsync over SSH to inventory hosts, with reuse of
--     recently-synced snapshots.
--   - Cloud source staging: R2 upload of the source tarball keyed by content
--     hash, extracted by the agent at instance startup.
--   - Data input classification: separating --input values into HF-style
--     asset refs vs. file-path overlays (including local: paths stitched
--     into the source snapshot).
--   - On-prem data prestaging: rsync HF models and datasets from a donor
--     host into the target host's HF cache before dispatch.
--   - Cloud data staging: intended prestaging of HF assets and producer
--     outputs to R2 so the agent starts with all declared inputs in place.
-- Excludes:
--   - Job lifecycle, placement and dispatch (job-lifecycle.allium)
--   - Campaign/instance lifecycle (campaign-lifecycle.allium)
--   - Status reconciliation and R2 progress markers (status-sync.allium)
--   - Output/artifact collection after job completion
--   - Agent deploy (cross-compiled binaries shipped separately from sources)
--
-- Guiding principle: weft arranges that every declared input a job needs is
-- already present at its expected path when the agent starts the command.
-- The command itself should never be the thing that fetches a declared
-- input — otherwise disk budgeting, bandwidth observations, and failure
-- reporting all lose signal.

use "./job-lifecycle.allium" as jobs
use "./campaign-lifecycle.allium" as cloud

-- ==========================================================================
-- External Entities
-- ==========================================================================

-- DispatchEntry is the on-host queue entry the dispatcher writes for a job.
-- Owned by the queue runner / dispatcher (internal/opsqueue), not by this
-- spec, but referenced here because the source-staging mode (rsync + per-job
-- marker vs. R2-isolated tarball) is recorded on it. source_sha256 and
-- source_r2_key live only on the queue entry, not on the persistent Job row.
external entity DispatchEntry {
    job: jobs/Job
    source_sha256: String?          -- SHA the dispatcher queued the job with
    source_r2_key: String?          -- "sources/<sha>.tar.gz" when R2-isolated
}

-- DataAsset identifies a piece of named, addressable data (an HF model,
-- dataset, or named corpus) that may exist on zero or more hosts. Managed
-- by the dataloc subsystem; this spec only observes its presence.
external entity DataAsset {
    kind: InputKind
    id: String                      -- e.g. "meta-llama/Llama-3-8B"
    ref: String                     -- canonical serialised form, e.g. "hf:<id>"
}

-- HostAssetEntry records that a given asset is present on a given inventory
-- host at a specific path, with a size last observed at last_seen. Populated
-- by HF-cache scans and corpus scans on each host.
external entity HostAssetEntry {
    host: jobs/Host
    asset: DataAsset
    path: String                    -- filesystem path on the host
    size_bytes: Integer             -- 0 if unknown
    last_seen: Timestamp
}

-- ==========================================================================
-- Value Types
-- ==========================================================================

-- How a declared --input value classifies for sync purposes.
-- hf_model / hf_dataset: fetched into a per-host HF cache directory.
-- checkpoint / corpus:   named assets that exist as on-host paths.
-- job_output:            produced by a previous weft job; resolved via
--                        dataloc or R2 staging depending on venue.
-- named_asset:           a content-addressed asset published via
--                        `weft data publish` (asset: prefix). Bytes live in
--                        R2 under assets/<content_hash> with a named_assets
--                        row mapping name → key + target path; stageable
--                        onto any host with R2 access.
-- local_path:            a path inside (or beneath) the job's working_dir
--                        that would otherwise be excluded; overlaid into
--                        the source snapshot so it ships with the source.
value InputKind = hf_model | hf_dataset | checkpoint | corpus
                | job_output | named_asset | local_path

-- A declared input, parsed from the user's --input flag or [tool.weft]
-- inputs list. Exactly one of asset or file_path is populated.
value InputRef {
    kind: InputKind
    raw: String                     -- original flag value, e.g. "hf:foo/bar"
    asset: DataAsset?               -- populated for asset kinds
    file_path: String?              -- populated for local_path kind
                                    -- (absolute or project-relative)
}

-- Where a source exclude pattern originated. Distinguishing origins lets
-- the checker report overrides coherently and keeps the default list
-- stable across projects.
value SourceExcludeOrigin = builtin | user_config | project_config
                          | gitignore | project_output_dir

value ExcludePattern {
    pattern: String
    origin: SourceExcludeOrigin
}

value SourceRootOrigin = project | explicit_config | derived_uv_sources

-- Outcome of the runner's per-job preflight provenance check. Distinct from
-- a runtime failure: the attempt never started, so there is no start_time,
-- no exit_code, no log header, and no meta file.
value PreflightOutcome = passed
                      | source_provenance_mismatch
                      | r2_isolated_source_unavailable

-- Record of a single observed source or data transfer, written to the
-- oplog and (for prestage) to the transferbw observation table.
value TransferObservation {
    source: String                  -- "host:<name>" or "r2"
    destination: String             -- "host:<name>" or "instance:<id>"
    bytes: Integer
    duration: Duration
    ok: Boolean
}

-- ==========================================================================
-- Contracts
-- ==========================================================================

-- The obligations the cloud agent relies on weft having discharged before
-- the agent begins executing a job. weft fulfils this contract by writing
-- the source tarball, the campaign manifest, and the job's declared inputs
-- to R2 under content-addressed keys; the agent demands it by refusing to
-- start a job whose declared inputs are not all resolvable.
contract AgentStagingContract {
    source_tarball_present: (source_hash: String) -> Boolean
	source_root_manifest_present: (manifest_hash: String) -> Boolean
    manifest_present: (instance_id: String) -> Boolean
    need_object_present: (r2_key: String) -> Boolean

    @invariant SourceHashMatchesManifest
        -- The source_hash referenced by the manifest served to the agent
        -- resolves to an object under the sources/ prefix whose content
        -- hashes to that value. The agent is entitled to reject mismatches
        -- as a hard failure.

	@invariant SourceRootManifestMatchesObjects
		-- Every root entry in the source_root_manifest resolves to an R2
		-- object under the sources/ prefix whose content hashes to that
		-- root's content_hash. The manifest hash is the source identity
		-- for the whole job; the root hashes are the dedupe identity for
		-- each mounted directory.

    @invariant NeedsResolvableBeforeStart
        -- Every CloudNeed entry in a job's manifest entry has an R2 key
        -- that exists by the time the agent begins that job. weft must
        -- not publish a manifest referencing objects it has not staged.

    @invariant RelaunchOutputsRestagedBeforeStart
        -- When a rental job attempt has predecessor_attempt_id and the
        -- predecessor uploaded convention-based output/artifact files to
        -- R2, the replacement attempt includes those files as CloudNeed
        -- entries in its manifest. The agent copies them into the new
        -- workdir before setup or command execution and sets
        -- WEFT_RESTAGED=1 for the job process.

    @invariant DeclaredInputsEnumerated
        -- The manifest enumerates both the source tarball and every
        -- cross-instance input the job depends on. The agent trusts the
        -- manifest; inputs omitted from it are invisible to the job.
}

-- ==========================================================================
-- Entities
-- ==========================================================================

-- A deterministic snapshot of a user's working directory at submission time,
-- with exclude rules and declared local_path overlays applied. Identified
-- by the SHA-256 of the gzipped tarball contents; the same tree always
-- produces the same hash. Snapshots are not persisted entities in the DB —
-- they exist as the R2 object `sources/<hash>` and as rsync state on each
-- host that has received them — but they are modelled here as an entity
-- because sync and reuse rules need to refer to "the snapshot for this
-- working dir at this time".
entity SourceSnapshot {
    working_dir: String             -- absolute local path at submission
    content_hash: String            -- hex SHA-256 of tarball
    size_bytes: Integer
    excludes: List<ExcludePattern>
    local_overlays: List<String>    -- relative paths that were overlaid
                                    -- back in (declared local_path inputs)
    built_at: Timestamp
}

-- One directory whose contents are part of a job's source identity. The
-- project is root 0 and mounts at the job working directory. Declared
-- sibling roots mount adjacent to it, preserving paths such as
-- `../research-expkit` on laptops, inventory hosts, and rentals.
entity SourceRoot {
	project_working_dir: String
	local_path: String
	mount_basename: String
	mount_relative_path: String     -- "." for root 0, "../<basename>" for siblings
	origins: List<SourceRootOrigin>
	snapshot: SourceSnapshot
	vcs_type: String?
	vcs_revision: String?
	vcs_change_id: String?
	vcs_dirty: Boolean?
}

-- Ordered source-root manifest for one submitted source state. The hash is
-- over (mount_basename, content_hash, r2_key) for each root in order, so
-- the job keeps one source identity while each root dedupes independently
-- in R2.
entity SourceRootManifest {
	project_working_dir: String
	roots: List<SourceRoot>
	manifest_hash: String
	built_at: Timestamp
}

-- SUPERSEDED by rsync incrementality (not implemented). HostSourceState was
-- intended to track what source snapshot is currently installed in a host's
-- sources directory so weft could skip redundant rsyncs via
-- ReuseRecentOnPremSnapshot. No such DB-side skip cache exists: rsync's delta
-- transfer already makes re-syncing an unchanged tree near-free, so weft
-- always rsyncs. The entity and its consuming rules are retained as the
-- documented intent rather than deleted, to avoid orphaning the per-job
-- marker and remediation rules that reference them. There is at most one
-- active snapshot per (host, working_dir) pair at a time.
entity HostSourceState {
    host: jobs/Host
    working_dir: String
    snapshot: SourceSnapshot
    synced_at: Timestamp
    rolling_marker_written: Boolean -- the rolling .weft-source.sha256 marker
                                    -- on the remote host, written after a
                                    -- successful rsync. Retained for human
                                    -- debugging only; preflight reads the
                                    -- per-job marker instead (see
                                    -- PerJobSourceMarker).
}

-- A per-job source provenance marker. Written by the dispatcher in the
-- target working directory at the SHA the job was queued against
-- (`.weft-source.<jobid>.sha256`). The runner reads this file at preflight
-- and rejects the attempt if the marker is missing, unreadable, or holds a
-- different SHA. Distinct from the rolling per-working-dir marker so that
-- a peer job rsyncing the same directory (or rsync --delete reaping the
-- old marker) cannot invalidate this job's preflight. Both the per-job
-- marker file pattern and the rolling marker file are in DefaultExcludes
-- so rsync --delete must not reap them.
entity PerJobSourceMarker {
    job: jobs/Job
    host: jobs/Host
    working_dir: String
    expected_sha: String            -- SHA the dispatcher queued the job at
    written_at: Timestamp
}

-- CloudSourceObject represents the R2-hosted tarball for a given source
-- snapshot. Content-addressed: one object per distinct content_hash,
-- shared across all instances that reference it.
entity CloudSourceObject {
    snapshot: SourceSnapshot
    r2_key: String                  -- e.g. "sources/<hash>.tar.gz"
    uploaded_at: Timestamp
}

-- ==========================================================================
-- Config
-- ==========================================================================

config {
    -- Size limit on an uncompressed source snapshot. Enforced before a
    -- tarball is written so a runaway working directory fails fast with
    -- a size report rather than filling up /tmp or R2.
    -- 500 MiB (binary), matching MaxSourceTarballBytes = 500 * 1024 * 1024
    -- in internal/sync/tarball.go.
    max_source_bytes: Integer = 524_288_000     -- 500 MiB

    -- Per-rsync deadlines.
    rsync_connect_timeout: Duration = 15.seconds   -- ssh connection window
    rsync_total_timeout: Duration = 30.seconds     -- one sync invocation
    rsync_cloud_ssh_timeout: Duration = 5.minutes  -- cloud instance over SSH

    -- SUPERSEDED by rsync incrementality. The 5-minute host-snapshot reuse
    -- window (and the HostSourceState skip path) was never implemented:
    -- rsync's own delta transfer makes a redundant re-sync of an unchanged
    -- working tree near-free, so weft always invokes rsync rather than
    -- maintaining a DB-side "recently synced" cache to skip it. Retained
    -- only because ReuseRecentOnPremSnapshot and HostSourceState reference
    -- it; see those declarations.
    host_snapshot_reuse_window: Duration = 5.minutes

    -- Maximum time the prestage orchestrator waits for all declared data
    -- transfers to an on-prem host before failing the dispatch. A separate
    -- timeout from rsync_total_timeout because prestage transfers are bulk
    -- and run over the cross-host fabric.
    prestage_transfer_timeout: Duration = 10.minutes

    -- R2 object-exists probe timeout. Used both when deciding whether to
    -- upload a source tarball and when verifying that a CloudNeed key is
    -- resolvable before publishing a manifest.
    r2_object_exists_timeout: Duration = 20.seconds

    -- Cloud-agent HF prewarm retry policy. Each attempt runs the whole
    -- download script under the setup timeout; transient Hub/network
    -- failures get bounded retries with a short backoff rather than
    -- failing the job on the first hiccup. See HFAssetsPrestagedOnCloud
    -- for the xet→classic fallback between attempts.
    -- Matches hfPrewarmMaxAttempts / hfPrewarmBackoff in cmd/agent/jobloop.go.
    hf_prewarm_max_attempts: Integer = 3
    hf_prewarm_backoff_after_first: Duration = 5.seconds
    hf_prewarm_backoff_later: Duration = 20.seconds

	-- Project-level roots that must be synced adjacent to working_dir.
	-- Explicit weft roots are read from .weft.toml [sync].sibling_roots
	-- only; PEP 723 [tool.weft] cannot configure this list.
	sibling_roots: List<String> = []
}

-- ==========================================================================
-- Defaults
-- ==========================================================================

-- The default source excludes ship with weft and are applied unless a
-- project explicitly overrides them. They cover VCS metadata, language
-- caches, editor state, and common build outputs, plus weft's own
-- convention-based output directories.
default ExcludePattern exclude_git       = { pattern: ".git",       origin: builtin }
default ExcludePattern exclude_jj        = { pattern: ".jj",        origin: builtin }
default ExcludePattern exclude_venv      = { pattern: ".venv",      origin: builtin }
default ExcludePattern exclude_node_mod  = { pattern: "node_modules", origin: builtin }
default ExcludePattern exclude_pycache   = { pattern: "__pycache__", origin: builtin }
default ExcludePattern exclude_pytest    = { pattern: ".pytest_cache", origin: builtin }
default ExcludePattern exclude_ds_store  = { pattern: ".DS_Store",  origin: builtin }
default ExcludePattern exclude_outputs   = { pattern: "output",     origin: builtin }
default ExcludePattern exclude_outputs_s = { pattern: "outputs",    origin: builtin }
default ExcludePattern exclude_testdata  = { pattern: "testdata",   origin: builtin }
default ExcludePattern exclude_env       = { pattern: ".env",       origin: builtin }
-- Source provenance markers are excluded so peer jobs' rsync --delete does
-- not reap this job's per-job marker (or the rolling per-workdir marker
-- retained for human debugging).
default ExcludePattern exclude_src_mark  = { pattern: ".weft-source.sha256",   origin: builtin }
default ExcludePattern exclude_src_mark_j = { pattern: ".weft-source.*.sha256", origin: builtin }

-- ==========================================================================
-- Rules: source snapshotting
-- ==========================================================================

-- Produce a content-addressed tarball for a submitted job's working
-- directory, applying default and project-specific excludes plus any
-- declared local_path overlays. The snapshot's hash is deterministic:
-- identical content yields identical hash regardless of mtimes.
rule BuildSourceSnapshot {
    when: BuildSnapshotRequested(working_dir, inputs)

    let excludes = DefaultExcludes ++ ProjectExcludes(working_dir)
                                  ++ GitignorePatterns(working_dir)
    let overlays = inputs where kind = local_path

    ensures: EnforceSourceSizeLimit(working_dir, excludes)

    ensures: SourceSnapshot.created(
        working_dir: working_dir,
        content_hash: HashTarball(working_dir, excludes, overlays),
        size_bytes: MeasureSize(working_dir, excludes),
        excludes: excludes,
        local_overlays: overlays.file_path,
        built_at: now
    )

    @guidance
        -- Implementation anchor:
        --   internal/sync/tarball.go       — CreateSourceTarball
        --   internal/sync/snapshot.go      — BuildSourceSnapshot
        --   internal/sync/sources.go       — sourceExcludes, parseGitignorePatterns
        -- Tarball entries are written with zero mtime/uid/gid so content
        -- hashes are stable across rebuilds and across machines.
}

-- Resolve the project root plus any configured sibling roots before a job is
-- queued. Explicit sibling roots come from .weft.toml. Derived sibling roots
-- come from [tool.uv.sources] path dependencies in the project's
-- pyproject.toml and in PEP 723 script metadata for the submitted command.
-- Sibling roots are deliberately narrower than general outside-root paths:
-- the same `../<basename>` reference must resolve on the submitter,
-- inventory hosts, and cloud rentals.
rule ResolveSourceRoots {
	when: BuildSourceRootManifestRequested(project_working_dir, inputs)

	let project_root = Abs(project_working_dir)
	let declared = ProjectConfig(project_root).sync.sibling_roots
	let derived = UVPathSources(project_root, SubmittedCommandScripts(project_root))

	ensures: SourceRoot.created(
		project_working_dir: project_root,
		local_path: project_root,
		mount_basename: Basename(project_root),
		mount_relative_path: ".",
		origins: [project],
		snapshot: LatestSnapshotFor(project_root)
	)

	ensures:
		for raw_root in declared:
			let root = AbsRelativeTo(raw_root, project_root)
			if not Exists(root):
				SubmissionRejected(
					working_dir: project_root,
					reason: sibling_root_missing,
					report: raw_root + " does not exist"
				)
			else if not IsDirectory(root):
				SubmissionRejected(
					working_dir: project_root,
					reason: sibling_root_not_directory,
					report: raw_root + " is not a directory"
				)
			else if Dirname(root) != Dirname(project_root):
				SubmissionRejected(
					working_dir: project_root,
					reason: sibling_root_not_sibling,
					report: raw_root + " is not a sibling of " + project_root
				)
			else if Basename(root) in MountBasenamesFor(project_root):
				SubmissionRejected(
					working_dir: project_root,
					reason: sibling_root_basename_collision,
					report: "source roots would both mount at " + Basename(root)
				)
			else:
				SourceRoot.created(
					project_working_dir: project_root,
					local_path: root,
					mount_basename: Basename(root),
					mount_relative_path: "../" + Basename(root),
					origins: [explicit_config],
					snapshot: BuildSourceSnapshot(root, no_overlays),
					vcs_type: DetectVCSType(root),
					vcs_revision: DetectVCSRevision(root),
					vcs_change_id: DetectVCSChangeID(root),
					vcs_dirty: DetectVCSDirty(root)
				)

	ensures:
		for dep in derived where PathEscapesProject(dep.path, project_root):
			let root = AbsRelativeTo(dep.path, dep.base_dir)
			if not Exists(root):
				SubmitWarning(
					working_dir: project_root,
					reason: derived_sibling_root_missing,
					report: dep.path + " from tool.uv.sources does not exist; it will not be synced"
				)
			else if not IsDirectory(root):
				SubmitWarning(
					working_dir: project_root,
					reason: derived_sibling_root_not_directory,
					report: dep.path + " from tool.uv.sources is not a directory; it will not be synced"
				)
			else if Dirname(root) != Dirname(project_root):
				SubmitWarning(
					working_dir: project_root,
					reason: derived_sibling_root_not_sibling,
					report: dep.path + " from tool.uv.sources is outside the project but is not a sibling; it will not be synced"
				)
			else if CanonicalPath(root) in CanonicalPaths(OrderedSourceRoots(project_root)):
				AddSourceRootOrigin(root: ExistingRootByCanonicalPath(root),
									origin: derived_uv_sources)
			else if Basename(root) in MountBasenamesFor(project_root):
				SubmitWarning(
					working_dir: project_root,
					reason: derived_sibling_root_basename_collision,
					report: dep.path + " from tool.uv.sources collides with an existing source-root mount; it will not be synced"
				)
			else:
				SourceRoot.created(
					project_working_dir: project_root,
					local_path: root,
					mount_basename: Basename(root),
					mount_relative_path: "../" + Basename(root),
					origins: [derived_uv_sources],
					snapshot: BuildSourceSnapshot(root, no_overlays),
					vcs_type: DetectVCSType(root),
					vcs_revision: DetectVCSRevision(root),
					vcs_change_id: DetectVCSChangeID(root),
					vcs_dirty: DetectVCSDirty(root)
				)

	@guidance
		-- Implementation anchor:
		--   internal/config/project.go      — ProjectSyncConfig.SiblingRoots
		--   internal/sync/source_roots.go   — ResolveSourceRoots,
		--                                     BuildSourceManifestForInputs
		--   internal/dataloc/uv_sources.go  — ScanUVPathSources
		--   internal/sync/vcs.go            — DetectVCSInfo
		-- Explicit .weft.toml sibling_roots are user requests to change the
		-- project's source composition, so invalid explicit paths are hard
		-- submit errors. Derived tool.uv.sources paths are dependency facts
		-- that uv already understands; syncing true siblings prevents the
		-- ModuleNotFoundError this feature addresses. Invalid derived paths
		-- warn and continue because a warning degrades current behavior,
		-- while an error would newly block submissions that used to be
		-- accepted. A root that is both explicit and derived is one root
		-- with both origins, deduped by canonical local path.
}

-- Reject attempts to configure sibling roots through PEP 723 [tool.weft].
-- The correct weft configuration home is .weft.toml [sync].sibling_roots.
rule RejectPEP723SiblingRootsConfig {
	when: ParsePEP723ToolWeft(script)
	requires: HasKey(script.tool.weft, "sibling-roots")
			  or HasKey(script.tool.weft, "sibling_roots")

	ensures: SubmissionRejected(
		working_dir: script.project_working_dir,
		reason: sibling_roots_in_script_metadata,
		report: "sibling-roots in script metadata is not supported; move it to .weft.toml"
	)

	@guidance
		-- sibling_roots defines what the project ships as source. A single
		-- submitted script must not be able to change the project source
		-- composition through [tool.weft]. [tool.uv.sources] is different:
		-- uv itself expects path dependencies there, including for standalone
		-- scripts, so weft derives sync roots from that dependency metadata
		-- rather than asking the user to duplicate it.
}

-- Build one content-addressed snapshot per resolved root and then hash the
-- ordered manifest. Only root 0 receives local_path overlays, because those
-- overlays are declared relative to the job working_dir.
rule BuildSourceRootManifest {
	when: BuildSourceRootManifestRequested(project_working_dir, inputs)

	requires: exists SourceRoot{project_working_dir: Abs(project_working_dir),
								mount_relative_path: "."}

	ensures:
		SourceRootManifest.created(
			project_working_dir: Abs(project_working_dir),
			roots: OrderedSourceRoots(Abs(project_working_dir)),
			manifest_hash: HashManifest(
				OrderedSourceRoots(Abs(project_working_dir))
					.map(mount_basename, snapshot.content_hash, CloudR2Key(snapshot))
			),
			built_at: now
		)

	@guidance
		-- Each root must go through the normal source snapshot path
		-- (`sourceExcludes`, DefaultExcludes, and .gitignore), never
		-- raw CopyPath, so sibling repos do not ship .git/.jj/.venv or
		-- language caches.
}

-- Persist per-root provenance with the queued job so `weft info` can answer
-- which project and sibling repository revisions the attempt used.
rule RecordSourceRootProvenance {
	when: JobQueued(job)

	let manifest = LatestSourceRootManifestFor(job.working_dir)

	ensures:
		JobMetadata(job).source.manifest_hash = manifest.manifest_hash
		for root in manifest.roots:
			JobMetadata(job).source.roots includes {
				local_path: root.local_path,
				mount_basename: root.mount_basename,
				mount_relative_path: root.mount_relative_path,
				origins: root.origins,
				content_hash: root.snapshot.content_hash,
				r2_key: CloudR2Key(root.snapshot),
				size_bytes: root.snapshot.size_bytes,
				vcs_type: root.vcs_type,
				vcs_revision: root.vcs_revision,
				vcs_change_id: root.vcs_change_id,
				vcs_dirty: root.vcs_dirty
			}
}

-- Submit and dry-run surfaces show every resolved root before launch so a
-- user can see accidental large siblings or diagnose a later import failure.
rule PrintResolvedSourceRoots {
	when: SubmitOrDryRun(job)

	let manifest = LatestSourceRootManifestFor(job.working_dir)

	ensures:
		if count(manifest.roots) > 1:
			SubmitOutputIncludes(
				heading: "Source roots:",
				rows: manifest.roots.map(
					mount_relative_path,
					CloudSourceRootDirForSubmitPreview(job, root),
					ShortSHA(root.snapshot.content_hash),
					root.origins
				)
			)
		for warning in SourceRootWarnings(job):
			SubmitOutputIncludes(warning)
}

-- Reject a snapshot build when the non-excluded working dir would exceed
-- the size limit. Emits a size report naming the largest contributors so
-- the user can add them to .gitignore / .weftignore.
rule EnforceSourceSizeLimit {
    when: EnforceSourceSizeLimit(working_dir, excludes)
    requires: MeasureSize(working_dir, excludes) > config.max_source_bytes

    ensures: SubmissionRejected(
        working_dir: working_dir,
        reason: source_too_large,
        report: LargestDirsAndFiles(working_dir, excludes)
    )
}

-- ==========================================================================
-- Rules: on-prem source sync
-- ==========================================================================

-- Send a snapshot to an inventory host over rsync+ssh. --delete is used so
-- remote sources mirror local sources exactly; the remote sources directory
-- is treated as weft's own scratch space, not a place the user edits.
--
-- After the rsync, the dispatcher writes TWO markers in the remote working
-- dir: the rolling `.weft-source.sha256` (human debug only) and the per-job
-- `.weft-source.<job-id>.sha256` carrying the SHA this job was queued at.
-- The per-job marker is what the runner's preflight reads (see
-- RunnerPreflightProvenanceCheck). Both filename patterns appear in
-- DefaultExcludes so a sibling rsync --delete cannot reap them.
rule SyncSourceToOnPremHost {
    when: DispatchToOnPremHost(job, host)
    requires: host.name != "" and not IsLocalHost(host)
    requires: job.working_dir != ""
    -- R2-isolated jobs bypass rsync entirely; their source is extracted
    -- from a content-addressed tarball into a per-job dir on the runner side.
    requires: DispatchEntry{job: job}.source_r2_key = null

	let manifest = LatestSourceRootManifestFor(job.working_dir)
	let snapshot = manifest.roots[0].snapshot

    ensures:
        if not HasRecentMatchingSnapshot(host, snapshot):
			for root in manifest.roots:
				RsyncSourceToHost(
					host: host,
					local_dir: root.local_path,
					remote_dir: HostSourceRootDir(host, job.working_dir, root),
					excludes: root.snapshot.excludes
				)
            HostSourceState.created(
                host: host,
                working_dir: job.working_dir,
                snapshot: snapshot,
                synced_at: now,
                rolling_marker_written: true
            )
            RecordTransferObservation(
                source: "local",
                destination: "host:" + host.name,
                bytes: snapshot.size_bytes,
                ok: true
            )
        -- Per-job marker stamps the SHA this job was queued at, regardless
        -- of whether the rsync ran or was skipped via the reuse window.
        PerJobSourceMarker.created(
            job: job,
            host: host,
            working_dir: job.working_dir,
            expected_sha: snapshot.content_hash,
            written_at: now
        )

    @guidance
        -- Implementation anchor:
        --   internal/sync/sync_to_host.go  — SyncSourcesToHost
        --   internal/sync/sources.go       — SyncSources, BuildRsyncArgs,
        --                                    DefaultExcludes
        --   internal/ops/host_sync.go      — per-job marker write at dispatch
        -- rsync uses BatchMode=yes ssh (see status-sync NonInteractiveSSH)
        -- so a fresh host key never blocks the TUI on a tty prompt.
}

-- SUPERSEDED by rsync incrementality (not implemented). This DB-side skip
-- path was never built; weft relies on rsync's delta transfer instead and
-- always invokes rsync. Retained as documented intent. Was: skip re-syncing
-- when the host already has this exact snapshot installed within the reuse
-- window, keyed on (host, working_dir, hash) so moving a job between two
-- projects in the same cwd does not reuse stale state.
rule ReuseRecentOnPremSnapshot {
    when: DispatchToOnPremHost(job, host)
    requires: exists HostSourceState{host: host, working_dir: job.working_dir}

    let state = HostSourceState{host: host, working_dir: job.working_dir}
    requires: state.snapshot.content_hash
              = LatestSnapshotFor(job.working_dir).content_hash
    requires: state.synced_at + config.host_snapshot_reuse_window > now
    requires: state.rolling_marker_written = true

    ensures: SourceSyncSkipped(host: host, job: job, reason: recent_match)
}

-- After an auto-remediation run or a user-requested `weft sync`, re-run the
-- sync unconditionally so stale state cannot paper over a missing file on
-- the remote side.
rule ResyncAfterRemediation {
    when: ForceSourceResync(host, working_dir)
    requires: working_dir != ""

	let manifest = LatestSourceRootManifestFor(working_dir)

	ensures:
		for root in manifest.roots:
			RsyncSourceToHost(
				host: host,
				local_dir: root.local_path,
				remote_dir: HostSourceRootDir(host, working_dir, root),
				excludes: root.snapshot.excludes
			)
    ensures:
        for state in HostSourceStates where host = host and working_dir = working_dir:
            state.synced_at = now
            state.rolling_marker_written = true
}

-- ==========================================================================
-- Rules: runner preflight (per-job source provenance)
-- ==========================================================================

-- Before the runner stamps a startTime, writes the meta or status files,
-- writes the log header, or emits OpJobStart, it reads the per-job source
-- marker the dispatcher wrote in the working dir and compares it against
-- the SHA recorded on the queue entry. A mismatch (or an unreadable
-- marker) is a preflight rejection: the attempt never started.
--
-- On rejection the runner writes two sidecar files in the agent's log
-- directory and emits OpJobStartFailed (NOT OpJobStart):
--   * <jobid>.failure_reason  — text content
--     "source_provenance_mismatch: expected=<X>, marker=<Y>"
--   * <jobid>.preflight_rejected — sentinel carrying the rejection ts
-- The status file, meta file, and log header are NOT written. The job
-- file is removed from the runner queue so the runner does not retry it.
-- The job remains queued in Weft's view, so the autopilot or
-- user can replan it.
--
-- R2-isolated jobs (queue entry has source_r2_key) skip the marker
-- comparison entirely: the SHA is implicit in the R2 key. The runner
-- instead invokes EnsureSourceFromR2 to extract the tarball into a
-- per-job source dir; failure to extract is itself a preflight rejection
-- with reason r2_isolated_source_unavailable.
rule RunnerPreflightProvenanceCheck {
    when: RunnerStartsJob(job, host)
    requires: job.target_kind = inventory_host

    let entry = DispatchEntry{job: job}
    let marker = PerJobSourceMarker{job: job, host: host,
                                    working_dir: job.working_dir}

    -- R2-isolated mode bypasses the marker comparison.
    if entry.source_r2_key != null:
        ensures:
            if EnsureSourceFromR2(job, entry.source_r2_key) succeeds:
                PreflightOutcomeRecorded(job: job, outcome: passed)
            else:
                PreflightRejected(
                    job: job,
                    outcome: r2_isolated_source_unavailable,
                    reason: "r2_isolated_source_unavailable: "
                            + entry.source_r2_key
                )

    -- Normal mode: per-job marker must exist and match.
    else if marker = null or marker.expected_sha != entry.source_sha256:
        ensures: PreflightRejected(
            job: job,
            outcome: source_provenance_mismatch,
            reason: "source_provenance_mismatch: expected="
                    + entry.source_sha256 + ", marker="
                    + (marker.expected_sha ?? "<missing>")
        )

    else:
        ensures: PreflightOutcomeRecorded(job: job, outcome: passed)

    @guidance
        -- Implementation anchor:
        --   internal/runner/runner.go    — startJob (preflight branch runs
        --                                  BEFORE startTime/meta/header/
        --                                  OpJobStart)
        --   internal/runner/runner.go    — rejectPreflight,
        --                                  WritePreflightRejectedFile
        --   internal/opsqueue/queue.go   — CommandJob.SourceSHA,
        --                                  CommandJob.SourceR2Key
}

-- Record a preflight rejection. No JobAttempt fields are mutated here;
-- the runner just writes the two sidecar files and removes the job
-- from its queue. The corresponding attempt closure on the sync side lives
-- in status-sync.allium (PreflightRejectionReconciled).
rule PreflightRejected {
    when: PreflightRejected(job, outcome, reason)

    ensures:
        PreflightFailureReasonWritten(job: job, reason: reason)
        PreflightSentinelWritten(job: job, rejected_at: now)
        OpJobStartFailedLogged(job: job, reason: reason)
        RunnerQueueFileRemoved(job: job)

    @guidance
        -- The agent's `batch-status` subcommand detects the sentinel and
        -- emits "JOB|<id>|PREFLIGHT_REJECTED|<ts>|<failure_reason>" on the
        -- wire, distinct from the COMPLETED line. See status-sync.allium
        -- § AgentBatchStatusReportsPreflightRejection for the wire
        -- contract and § PreflightRejectionReconciled for what the
        -- sync path does with it.
}

-- ==========================================================================
-- Rules: Layer D — R2-isolated source fallback
-- ==========================================================================

-- When a job has prior dispatch evidence of source-related failure, the
-- dispatcher escalates to R2-isolated mode rather than rsyncing the working
-- dir again. The escalation is per-job and runs at the next dispatch sweep,
-- not from the runner side.
--
-- Two triggers, evaluated in priority order:
--   (1) provenance-mismatch trigger: at least one prior
--       EventQueueDispatchFailed for this job whose detail starts with
--       "source_provenance_mismatch". This event is emitted only by the new
--       PREFLIGHT_REJECTED protocol. The detail carries the expected SHA, so
--       the dispatcher can branch on whether the laptop tree can still
--       reproduce that SHA:
--         - matches  → upload the source tarball to R2 under
--                      sources/<sha>.tar.gz (idempotent; same data plane as
--                      cloud rentals), stamp source_r2_key on the queue
--                      entry, skip the rsync and the per-job marker write
--                      for this dispatch.
--         - mismatch → the SHA has been lost (working dir advanced past the
--                      SHA this job was queued at). Surface as
--                      provenance_pin_lost rather than loop silently, so
--                      diagnose has a concrete reason to show.
--   (2) legacy-attempt trigger: at least one prior JobAttempt for this job
--       ended with status in {failed, dead} (or terminal failure exit code).
--       This catches jobs that failed under pre-PREFLIGHT_REJECTED runners,
--       which reported source-provenance failures only via the runner log
--       and a non-zero exit, with no lifecycle event the dispatcher can read.
--       No expectedSHA is recoverable, so the dispatcher unconditionally
--       uploads the current laptop SHA and runs R2-isolated. The job may run
--       against newer sources than originally queued; the alternative is
--       wedging forever waiting for a marker the new runner protocol never
--       generated.
--
-- Trigger (1) takes precedence when both apply: its branch produces a richer
-- diagnosis (pin_lost vs. matching SHA) and avoids the legacy fallback's
-- "run against current sources" semantics when the dispatcher has enough
-- information to do the strict check.
rule EscalateToR2IsolatedSource {
    when: DispatchToOnPremHost(job, host)
    requires: job.target_kind = inventory_host

    let prov_fails = count(LifecycleEvents
                           where event_kind = queue_dispatch_failed
                                 and job = job
                                 and detail starts_with "source_provenance_mismatch")
    let failed_attempts = count(JobAttempts
                                where job = job
                                      and end_time != null
                                      and (status in {failed, dead}
                                           or (status = completed and exit_code != 0)))

    requires: prov_fails >= 1 or failed_attempts >= 1

    ensures:
        if prov_fails >= 1:
            -- Trigger (1): provenance-mismatch path. The expected SHA isn't
            -- stored on the job record — it lives in the failure detail of
            -- the prior dispatch event. Parse it out so we can decide
            -- whether the laptop tree can still reproduce it.
            let expected_sha = ExtractExpectedSHA(
                LatestQueueDispatchFailedDetail(job, "source_provenance_mismatch")
            )
            let current_sha = ComputeSourceSHA256(job.working_dir)

            if expected_sha = null:
                -- Prior failure detail lacked an expected SHA. Cannot
                -- escalate via this path; fall through to normal dispatch.
                EscalationSkipped(job: job, reason: no_expected_sha)
            else if current_sha = null:
                -- Local hash failed (unreadable working dir, etc.). Cannot
                -- escalate; let normal path try.
                EscalationSkipped(job: job, reason: hash_unavailable)
            else if current_sha != expected_sha:
                -- Pin lost. Record a fresh dispatch-failed event and skip
                -- dispatch for this job in this sweep.
                EventQueueDispatchFailedRecorded(
                    job: job,
                    detail: "provenance_pin_lost: queued at "
                            + ShortSHA(expected_sha)
                            + ", laptop now " + ShortSHA(current_sha)
                )
                DispatchSkippedForJob(job: job)
            else:
                -- Pin still reproducible: upload to R2 and switch the queue
                -- entry to R2-isolated mode for this dispatch.
                let key = UploadSourceToR2ForInputs(
                    local_dir: job.working_dir,
                    inputs: job.inputs
                )
                DispatchEntry{job: job}.source_r2_key = key
                EscalationLogged(
                    job: job, host: host, r2_key: key,
                    reason: "prior provenance failure"
                )
        else:
            -- Trigger (2): legacy failed-attempt path. No expected SHA is
            -- recoverable, so upload the current laptop SHA and run
            -- R2-isolated. Accept that the job may run against newer sources
            -- than it was originally queued at — that matches user intent
            -- better than wedging forever.
            let key = UploadSourceToR2ForInputs(
                local_dir: job.working_dir,
                inputs: job.inputs
            )
            DispatchEntry{job: job}.source_r2_key = key
            EscalationLogged(
                job: job, host: host, r2_key: key,
                reason: "legacy failed attempt"
            )

    @guidance
        -- Implementation anchor:
        --   internal/ops/host_sync.go    — Layer D escalation block
        --                                  (escalateToR2 closure, the
        --                                   provFails/failedAttempts switch)
        --   internal/db/lifecycle_events.go — CountJobDispatchFailuresMatching
        --                                     (trigger 1), CountJobFailedAttempts
        --                                     (trigger 2)
        --   internal/sync/provenance.go  — ComputeSourceSHA256
        --   internal/sync/r2upload.go    — UploadSourceToR2ForInputs
        --   internal/opsqueue/queue.go   — QueueEntry.SourceR2Key
        --   internal/opsqueue/commandqueue.go — CommandJob.SourceR2Key
        --
        -- source_r2_key lives only on the in-process queue entry and the
        -- on-host command-queue file, not on the persistent Job row.
        -- This mirrors the precedent set by source_sha256 (also not in
        -- the DB) and keeps escalation a per-dispatch decision rather
        -- than a sticky job attribute.
}

-- ==========================================================================
-- Rules: cloud source staging
-- ==========================================================================

-- Upload each source root tarball for a job's source-root manifest to R2
-- under its own content-addressed key. Skipped when an object with that
-- key already exists, which is the common case for repeated submissions
-- from an unchanged sibling repo.
rule UploadSourceToCloud {
    when: CloudStagingRequested(job)
    requires: job.working_dir != ""

	let manifest = LatestSourceRootManifestFor(job.working_dir)

    ensures:
		for root in manifest.roots:
			let key = "sources/" + root.snapshot.content_hash + ".tar.gz"
			if not R2ObjectExists(key):
				R2Put(key: key, body: TarballFor(root.snapshot), content_type: "application/gzip")
				CloudSourceObject.created(
					snapshot: root.snapshot,
					r2_key: key,
					uploaded_at: now
				)
				RecordTransferObservation(
					source: "local",
					destination: "r2",
					bytes: root.snapshot.size_bytes,
					ok: true
				)

    @guidance
        -- Implementation anchor:
		--   internal/sync/r2upload.go     — UploadSourceRootsToR2WithProgressForInputs
        --   internal/dataplane/keys.go    — SourceTarball key helper
}

-- When a cloud instance reaches the sources_extracted bootstrap stage, the
-- agent has pulled each sources/<hash>.tar.gz named in the manifest and
-- expanded it into that root's mount directory. After that point, per-job
-- dispatches on that instance reuse the already-extracted trees and repair
-- any missing mount before setup or command execution.
rule StageSourceOnCloudInstance {
    when: instance: cloud/Instance.bootstrap_stage
          transitions_to status_sync/sources_extracted

	requires: instance.campaign_manifest.source_root_manifest != null

	ensures:
		for root in instance.campaign_manifest.source_root_manifest.roots:
			InstanceSourceRootReady(
				instance: instance,
				source_hash: root.snapshot.content_hash,
				remote_dir: CloudSourceRootDir(instance, root)
			)

	@guidance
		-- The R2 keys the agent pulls are written by weft via
		-- UploadSourceToCloud before the manifest is published; see
		-- AgentStagingContract. The agent repair path must test every
		-- mounted root, not only the project directory, because a wiped
		-- sibling still leaves root 0 looking populated.
}

-- The cloud extraction primitive owns exactly one target directory and may
-- remove it before untarring to avoid merging two stale trees. Therefore a
-- source object must never contain both the project and its siblings rooted
-- at their parent directory.
rule SourceRootExtractTargetIsExclusive {
	when: ExtractCloudSourceRoot(instance, root)

	ensures: RemoveAllBeforeExtract(target_dir: CloudSourceRootDir(instance, root))
	ensures: TarballFor(root.snapshot).contains_only_paths_beneath(".")

    @guidance
		-- A fused tarball rooted at the parent would contain both
		-- mental-spaces/ and research-expkit/ and would need extraction at
		-- /workspace. The existing safe extraction primitive removes the
		-- target directory first; pointed at /workspace, that would delete
		-- unrelated project trees on the rental. A fused tarball nested
		-- under the project avoids deletion but changes uv paths from
		-- ../research-expkit to .weft/siblings/research-expkit. Split
		-- tarballs preserve both deletion scope and identical relative
		-- paths across laptop, inventory host, and rental.
}

-- ==========================================================================
-- Rules: data input classification and on-prem prestaging
-- ==========================================================================

-- Split a job's --input list into parsed InputRefs before placement and
-- dispatch can use them. Parsing failures are surface-level errors, not
-- silent drops — an unrecognised prefix aborts submission.
rule ClassifyInputs {
    when: ClassifyInputsRequested(job)

    ensures:
        for raw in job.inputs:
            InputParsed(job: job, ref: ParseInputRef(raw))

    @guidance
        -- Implementation anchor:
        --   internal/dataloc/types.go  — ParseInputRef, ClassifyInputs
        -- Prefixes: hf:, hf-dataset:, checkpoint:, corpus:, job-output:,
        --           asset: (named_asset), local:
}

-- Declared file-path inputs that live outside working_dir (e.g.
-- ~/sources/vidur/data/) are rsynced to the same absolute path on the
-- target host before dispatch. Paths inside working_dir use the local_path
-- overlay path instead (see BuildSourceSnapshot).
rule SyncExtraFilePaths {
    when: DispatchToOnPremHost(job, host)
    requires: not IsLocalHost(host)

    let extras = job.inputs where kind = local_path and not IsInsideWorkingDir(file_path, job.working_dir)

    ensures:
        for input in extras:
            RsyncExtraPath(host: host, path: input.file_path)

	@guidance
		-- This rule remains on-prem-only for ad hoc outside-root local:
		-- file inputs. Declared project sibling roots are not extras; they
		-- are SourceRoot entries and reach cloud rentals via
		-- UploadSourceToCloud.
}

-- Before dispatch to an on-prem host, transfer any HF-cache assets the job
-- declares that are missing on that host but present on some donor host in
-- the inventory. This keeps job startup time off the critical path and
-- makes the download visible to the planner (for cost and bandwidth
-- accounting) instead of buried inside user code.
rule PrestageMissingAssetsOnPrem {
    when: DispatchToOnPremHost(job, host)
    requires: not IsLocalHost(host)

    let hf_inputs = job.inputs where kind in {hf_model, hf_dataset}
    let missing = hf_inputs where not AssetPresentOn(asset, host)

    ensures:
        for input in missing:
            let donor = BestDonorFor(input.asset)
            if donor != null:
                RequireHFCacheDir(host: host, asset: input.asset)
                RsyncAssetBetweenHosts(
                    source_host: donor,
                    target_host: host,
                    asset: input.asset,
                    timeout: config.prestage_transfer_timeout
                )
                RecordTransferObservation(
                    source: "host:" + donor.name,
                    destination: "host:" + host.name,
                    bytes: SizeOn(donor, input.asset),
                    ok: true
                )

    @guidance
        -- Implementation anchor:
        --   internal/prestage/prestage.go  — BuildPlan, Execute
        -- Push-then-pull fallback: if the donor can't reach the target,
        -- target SSHes into donor and pulls.
}

-- Guard: prestaging to a target host requires knowing where that host's
-- HF cache lives. If hf_cache_dir is not configured, fail fast and
-- direct the user to `weft host discover <host>` rather than dumping
-- files in an arbitrary location.
rule RequireHFCacheDir {
    when: RequireHFCacheDir(host, asset)
    requires: HFCacheDirFor(host) = null

    ensures: PrestageAborted(
        host: host,
        asset: asset,
        reason: hf_cache_dir_not_configured,
        hint: "run 'weft host discover " + host.name + "'"
    )
}

-- A declared input on a job dispatched to the local machine (the studio
-- coordinator host itself, for example) does not need prestaging — the
-- asset either already lives in the local HF cache or will be downloaded
-- to it by the command using its standard tooling. The spec records this
-- case explicitly rather than silently relying on the local_host skip
-- inside the rsync path.
rule DownloadHFAssetOnDemand {
    when: DispatchToOnPremHost(job, host)
    requires: IsLocalHost(host)

    let hf_inputs = job.inputs where kind in {hf_model, hf_dataset}

    ensures:
        for input in hf_inputs:
            if not AssetPresentOn(input.asset, host):
                LocalHFDownloadExpected(job: job, asset: input.asset)
}

-- corpus: inputs and checkpoints that are not confirmed in R2 are
-- non-transportable: they name large on-host paths that weft never
-- byte-transports between hosts (no donor rsync, no R2 round-trip). Instead
-- they pin placement — a host is eligible for the job only if the dataloc
-- inventory records that host as holding the asset. This is a hard
-- eligibility filter, applied before scoring, not a soft locality preference.
-- A job whose non-transportable input exists on no host at all is unplaceable
-- and surfaces that reason.
-- (This is the wb28 fix: previously such jobs were placed on an arbitrary
-- host and failed at runtime with missing_file.)
rule NonTransportableInputsPinPlacement {
    when: PlacementEvaluated(job, host)

    let pinned = job.inputs where
        kind = corpus or
        (kind = checkpoint and TransportState(input.asset) != r2_resident)

    ensures:
        for input in pinned:
            if not AssetPresentOn(input.asset, host):
                HostIneligible(
                    host: host,
                    job: job,
                    reason: "missing " + input.asset.ref
                            + " on this host; present on "
                            + HoldersOf(input.asset)
                )

    @guidance
        -- Implementation anchor:
        --   internal/placement/placement.go — hard-filter call in scoreHost
        --     (missingNonTransportableInputReason), the reason builder
        --     missingNonTransportableInputReason, and the kind predicate
        --     isNonTransportableInput (checkpoint | corpus).
        -- Published checkpoints (host_data.content_hash with a matching
        -- named_assets row) are not hard-pinned in on-prem scoring; the
        -- holder host remains preferred through normal data-locality transfer
        -- scoring.
        -- The eligibility mechanics of placement live in
        -- inventory-placement.allium; this rule pins down which input
        -- kinds are placement constraints rather than staging work.
}

value TransportState = r2_resident | on_host_only | unknown

rule R2ResidentCheckpointIsTransportable {
    when: RentalPlacementEvaluated(job)

    for input in job.inputs where input.kind = checkpoint:
      let record = HostAssetRecordFor(input.asset)
      let key = "assets/" + record.content_hash
      let state = ProbeR2Object(key)

      ensures:
        if state = r2_resident:
            RentalEligibleWithStaging(job, input.asset)
            ManifestCloudNeedRecorded(
                job: job,
                spec: input.asset.ref,
                path: TargetPathForPublishedCheckpoint(input.asset),
                r2_key: key
            )
        if state = on_host_only:
            RentalIneligible(job, reason: "checkpoint not confirmed in R2")
        if state = unknown:
            RentalIneligible(job, reason: "checkpoint R2 state unknown")

    @guidance
        -- Three-state discipline is required here. A successful R2 HEAD that
        -- finds the object makes the checkpoint transportable; a successful
        -- miss is confirmed on-host-only; any timeout, auth failure, or other
        -- probe error is unknown. Unknown fails closed for rental placement
        -- and must not be collapsed to either "not in R2" or "transportable".
        -- Implementation anchors:
        --   internal/campaign/checkpoint_transport.go
        --   internal/campaign/cloud_provision_guard.go
        --   internal/campaign/lifecycle.go and reuse.go append the CloudNeed
        --   cmd/agent/cloud_needs.go extracts directory archives when
        --     CloudNeed.content_type = directory.
        -- Directory checkpoints are staged as deterministic tar.gz archives
        -- keyed by the directory manifest digest described in
        -- inventory-placement.allium DirectoryAssetDigest, then extracted at
        -- the consumer target path.
}

rule RegisteredCheckpointPublishHostToR2 {
    when: DataPublishRequested(source = checkpoint_ref)

    let record = HostAssetRecordFor(checkpoint_ref)
    let key = "assets/" + record.content_hash

    ensures:
        if ProbeR2Object(key) = r2_resident:
            NamedAssetRecordUpserted(checkpoint_ref, key)
        if ProbeR2Object(key) = on_host_only:
            R2PutFromHost(
                source_host: record.host,
                source_path: record.path,
                key: key,
                content_type: record.content_type
            )
            NamedAssetRecordUpserted(checkpoint_ref, key)
        if ProbeR2Object(key) = unknown:
            PublishFails(reason: "R2 status unknown")

    @guidance
        -- A remote holder checkpoint must upload from the holder host
        -- directly to R2. The controller may probe R2 and issue SSH commands,
        -- but it must not SCP the checkpoint to the laptop and then upload
        -- from the laptop. Directory checkpoints use the same deterministic
        -- tar.gz stream as local publish: WriteDirectoryArchive over the
        -- sorted manifest digest in inventory-placement.allium
        -- DirectoryAssetDigest. Single files stream as application/octet-stream.
        --
        -- Credential scope: the ideal mechanism is a temporary credential
        -- constrained to the content-addressed named-asset key prefix. The
        -- current implementation has no Cloudflare token-minting API or
        -- per-key credential broker, so it passes the configured R2 key
        -- transiently to the one SSH invocation of `weft r2 put-content
        -- --creds-stdin`, piped as a JSON blob on the process's stdin. It must
        -- NOT place the credentials in the remote command's argv or environment
        -- (both are readable via `ps` by any user on the holder host during the
        -- upload window) and must not write them to a remote config file or
        -- rclone profile. Operators who can provide a narrower R2 key should
        -- configure that key as the Weft R2 credential.
        --
        -- A host-unreachable or SSH failure is unknown: publish must fail
        -- clearly and leave the named-asset/checkpoint mapping unchanged unless
        -- the upload was already confirmed in R2. The implementation must not
        -- silently fall back to laptop-transit, because that reintroduces the
        -- large-data failure mode this rule prevents.
        --
        -- Implementation anchors:
        --   cmd/data.go runDataPublish / remotePublishSource
        --   cmd/r2.go r2 put-content / r2 content-info
        --   internal/dataloc/r2put.go R2PutFromHost
        --   internal/campaign/checkpoint_transport.go PublishCheckpointToR2
}

-- ==========================================================================
-- Rules: cloud data staging
-- ==========================================================================

-- SPECIFIED-BUT-UNIMPLEMENTED for producer-output `--needs` staging. v2
-- implements R2PutFromHost for data publish/checkpoint staging, but this
-- producer-output resolution path is not yet wired to call it. A cloud
-- consumer that depends on an on-prem-produced (or previously-completed)
-- output it cannot co-locate with still fails with ErrArtifactMissing
-- instead of having the artifact staged into R2. This rule is the intended
-- design for that remaining use case; do not delete it. The companion
-- on-prem path (OnPremStagingFromR2, below) is the implemented direction.
--
-- Producer job outputs that a consumer depends on via `--needs` and that
-- are no longer reachable via same-instance co-location must be staged
-- into R2 at a content-addressed key and referenced from the consumer's
-- manifest entry as a CloudNeed. This is the only path by which a cloud
-- job consumes an on-prem-produced or previously-completed output.
rule CloudDataStagingViaR2 {
    when: CloudNeedResolutionRequested(job, need_spec)
    requires: job.target_kind = rental_instance
    requires: not ProducerCoLocatedWith(job, need_spec)

    let producer_output = ResolveProducerOutput(need_spec)
    let key = "needs/" + ContentHashOf(producer_output)

    ensures:
        if not R2ObjectExists(key):
            R2PutFromHost(
                source_host: producer_output.host,
                source_path: producer_output.path,
                key: key
            )
        ManifestCloudNeedRecorded(
            job: job,
            spec: need_spec,
            path: producer_output.consumer_path,
            r2_key: key
        )

    @guidance
        -- NOT YET WIRED FOR PRODUCER OUTPUTS: R2PutFromHost exists for
        -- data publish/checkpoint staging, but this `--needs` path does not
        -- yet call it. Until it is wired, cloud consumers of unco-located on-prem
        -- artifacts fail with ErrArtifactMissing. The anchors below
        -- describe where the implementation is intended to live / partly
        -- lives for the consumer-side manifest plumbing:
        --   internal/cloudneeds/            — spec parsing and producer lookup
        --   internal/cloud/wrapper.go CloudNeed struct — manifest entry shape
        -- The agent reads cloud_needs from the manifest and rclone-copies
        -- each r2_key into path before starting the job.
}

-- Companion rule for on-prem consumers: when an on-prem-bound consumer
-- declares `--needs` against a producer that ran on a rental, the producer's
-- output is not on the consumer's host filesystem, and there is no
-- producer queue runner co-located with the consumer to write the satisfied
-- marker that CheckDependencies looks for. The host-sync dispatcher fills
-- the gap by staging the artifact from R2 onto the host before the consumer
-- runs, and writing the satisfied marker the on-prem queue runner expects.
--
-- Without this rule the consumer would sit in pending forever waiting for a
-- marker that nothing writes (the wj1583 shape).
rule OnPremStagingFromR2 {
    when: OnPremDispatchTick(host)
    requires: host.target_kind = inventory_host

    for job in host.queued_jobs:
      for need in job.needs:
        let producer = ResolveProducerJob(need)
        requires: producer.target_kind = rental_instance

        let key = ResolveProducerR2Key(producer, need.path)

        ensures:
            ArtifactBytesPresentOnHost(host, job.working_dir / need.path)
            SatisfiedMarkerWritten(host, need.version, need.path, exit_code: 0)

    @guidance
        -- Implementation anchor:
        --   internal/ops/host_sync.go stageArtifactNeedsFromR2  — staging
        --   internal/r2resolve/             — R2 key resolution (shared)
        -- Idempotent: needs whose marker already exists are skipped, and a
        -- size-equality check guards against an interrupted scp leaving a
        -- truncated file at the final path.
        -- The atomic-rename pattern (scp to <path>.weft-staging then mv) keeps
        -- a killed transfer from being mistaken for a complete one on the
        -- next sync cycle.
        --
        -- Per-(host, marker) DB lease (`needs-stage:<host>:<markerName>`,
        -- 15m TTL) gates each transfer so that overlapping callers
        -- (autopilot tick + list-TUI + watch-TUI) cannot spawn concurrent
        -- scp's into the same `.weft-staging` destination. Without it, a
        -- 500MB-class artifact has been observed accumulating three
        -- simultaneous half-finished uploads that stomped each other.
}

-- Named assets extend OnPremStagingFromR2 to non-producer-job sources:
-- the bytes were published via `weft data publish` rather than produced by
-- a rental job, so there is no producer JobAttempt to resolve. The chain is:
--   publish → named_assets row (name, content_hash, target_path)
--   → `--input asset:NAME` mirrored into `--needs asset:NAME` at submission
--   → needs resolution reads the named_assets row and yields the
--     pre-computed R2 key assets/<content_hash>
--   → the host-sync dispatcher stages the bytes onto the host at
--     target_path and writes the satisfied marker before dispatch. Directory
--     assets are stored as deterministic tar.gz archives and extracted into
--     target_path.
-- This is the wb29 fix: previously asset: inputs were neither staged nor
-- blocking, so the command ran and failed with missing_file.
-- Submit validates every referenced asset:NAME against named_assets before
-- recording the job; a path-like value such as asset:output/foo.jsonl is not
-- accepted unless it is literally the published asset name.
rule OnPremNamedAssetStagingFromR2 {
    when: OnPremDispatchTick(host)
    requires: host.target_kind = inventory_host

    for job in host.queued_jobs:
      for need in job.needs where IsNamedAssetSpec(need):
        let asset_row = NamedAssetByName(NameOf(need))

        ensures:
            ArtifactBytesPresentOnHost(host, job.working_dir / asset_row.target_path)
            SatisfiedMarkerWritten(host, NameOf(need), asset_row.target_path,
                                   exit_code: 0)

    @guidance
        -- Implementation anchor:
        --   cmd/run.go                        — auto-mirror of --input
        --                                       asset:NAME into --needs
        --   internal/ops/host_sync.go         — parseAssetNeedSpec,
        --     collectPendingNeeds (named-asset branch resolving
        --     db.GetNamedAssetByName → pendingNeed.preResolvedR2Key =
        --     r2keys.NamedAsset(content_hash)), stageArtifactNeedsForHost,
        --     stageMissingNeeds
        -- Shares the probe/lease/atomic-rename machinery of
        -- OnPremStagingFromR2; the only difference is key resolution
        -- (named_assets row instead of producer-attempt R2 lookup).
}

-- HF models and datasets declared on a cloud-bound job are staged by the
-- cloud agent into the instance's HF cache before the job command starts.
-- The staging step is per-job, so failures are attributed to the job
-- attempt rather than hidden inside instance bootstrap. The agent may
-- prewarm the next job's declared HF inputs while the current job is
-- running, subject to dependency and benchmark barriers.
rule HFAssetsPrestagedOnCloud {
    when: CloudStagingRequested(job)
    requires: job.target_kind = rental_instance

    let hf_inputs = job.inputs where kind in {hf_model, hf_dataset}

    ensures:
        for input in hf_inputs:
            CloudAssetStaged(
                instance: job.cloud_instance,
                asset: input.asset,
                via: agent_hf_download
            )
        JobCommandStarts(job) only_after CloudHFInputsReady(job)

    @guidance
        -- Worker bootstrap starts the agent after installing sources; it
        -- does not front-load all HF downloads. Donor bootstrap may still
        -- populate a reusable HF cache. Implementation: cmd/agent
        -- setupPrewarmManager downloads hf:/hf-dataset: inputs before
        -- RunSingleJob, and can do this in the background for the next
        -- eligible job once the current job reaches the running phase.
        -- The managed HF staging subprocess must be online-capable even when
        -- the job runtime requests offline mode. Preserve auth/cache/proxy
        -- env such as HF_TOKEN, HF_HOME, and HTTPS_PROXY, but override
        -- HF_HUB_OFFLINE / TRANSFORMERS_OFFLINE / HF_DATASETS_OFFLINE only
        -- for staging. The job command itself still receives the user env.
        --
        -- Retry semantics: the download runs up to
        -- config.hf_prewarm_max_attempts times, each under the setup
        -- timeout, backing off hf_prewarm_backoff_after_first after the
        -- first failure and hf_prewarm_backoff_later thereafter. If the
        -- prewarm log shows an xet writer/reconstruction error ("file
        -- reconstruction error", "internal writer error", "background
        -- writer channel closed"), subsequent attempts set
        -- HF_HUB_DISABLE_XET=1 to fall back to the classic HTTP transfer
        -- path. Exhausting all attempts is an infrastructure failure
        -- (infra_prewarm_download_failed), not a user-code failure, for
        -- explicit inputs declared through --input, project config, or
        -- PEP-723 [tool.weft] inputs. HF refs contributed only by
        -- source/command auto-detection are best-effort. Submission and
        -- retry refresh must not add auto-detected HF refs when any inputs
        -- were explicitly declared, and auto-detected HF refs that lack the
        -- org/name repo shape are discarded before staging. After the same
        -- retry/xet-disable sequence fails for a remaining best-effort ref,
        -- the agent logs a warning and starts the job. If the command truly
        -- needs that ref, user code fails at runtime; a spurious argparse
        -- default must not block a job with a different explicit model.
        -- Implementation:
        -- cmd/agent/jobloop.go runHFDownloadPrewarm, hfPrewarmBackoff,
        -- hfPrewarmTextHasXetWriterError.
}

rule CloudPrewarmRespectsJobOrder {
    when: AgentRunsCampaign(instance)

    -- Ordering is a soft optimization except for data dependencies. Among
    -- jobs with no dependency relation, the agent prefers:
    --   1. higher job priority
    --   2. jobs without slow setup/prewarm work
    --   3. original manifest order
    -- A job that needs an artifact from another same-instance job must not
    -- be moved before its producer.

    ensures:
        for producer, consumer in same_instance_data_dependencies(instance):
            index(producer) < index(consumer)

    @guidance
        -- Data dependency edges come from CloudAfter and --needs specs.
        -- Slow setup includes declared HF inputs and detected setup
        -- commands such as uv sync that are not skipped by script metadata.
        -- Benchmark jobs are barriers for background prewarm.
}

-- Every source or data transfer weft performs gets a TransferObservation
-- recorded, whether it succeeded or not. Observations feed the transferbw
-- estimator and show up in the oplog for manual debugging.
rule RecordTransferObservation {
    when: RecordTransferObservation(source, destination, bytes, ok)

    ensures: TransferObservationLogged(
        source: source,
        destination: destination,
        bytes: bytes,
        duration: MeasureElapsed,
        ok: ok,
        at: now
    )
}

-- ==========================================================================
-- Invariants
-- ==========================================================================

-- The SHA-256 of a source tarball is a pure function of (file contents +
-- paths + exclude set + overlay set). Mtimes, uids, gids, and file order
-- within the tarball do not enter. Two submissions from identical source
-- trees must produce byte-identical tarballs and therefore identical hashes.
invariant SourceHashDeterministic {
    for s1, s2 in SourceSnapshots:
        (s1.working_dir = s2.working_dir
         and s1.excludes = s2.excludes
         and s1.local_overlays = s2.local_overlays
         and SameTreeContents(s1.built_at, s2.built_at, s1.working_dir, s1.excludes))
        implies s1.content_hash = s2.content_hash
}

-- Declared sibling roots must be true siblings of the project root. This is
-- what makes `../<basename>` resolve the same way locally, on inventory
-- hosts, and under /workspace on rentals.
invariant SiblingRootsAreTrueSiblings {
	for root in SourceRoots where root.mount_relative_path != ".":
		Dirname(root.local_path) = Dirname(root.project_working_dir)
		and root.mount_relative_path = "../" + root.mount_basename
}

-- Source-root mount basenames are unique within a manifest. A duplicate
-- basename would mount two roots into the same remote directory; the
-- second extract or rsync --delete would silently wipe the first.
invariant SourceRootBasenamesUnique {
	for manifest in SourceRootManifests:
		Unique(manifest.roots.mount_basename)
}

-- Explicit and derived declarations of the same canonical local path are one
-- source root with multiple origins, not a basename collision.
invariant SourceRootsDedupeByCanonicalPath {
	for manifest in SourceRootManifests:
		Unique(manifest.roots.map(CanonicalPath(local_path)))
		and for root in manifest.roots:
			root.origins = AllOriginsForCanonicalPath(root.local_path)
}

-- The source-root manifest hash is the job-level source identity. It is a
-- pure function of the ordered root mount basenames and each root's
-- content-addressed R2 key.
invariant SourceRootManifestHashDeterministic {
	for manifest in SourceRootManifests:
		manifest.manifest_hash =
			HashManifest(manifest.roots.map(mount_basename,
											snapshot.content_hash,
											CloudR2Key(snapshot)))
}

-- The rolling marker flag on a HostSourceState must only be true after
-- the corresponding rsync has returned success. Marker-before-success is
-- a safety hazard: a subsequent dispatch would skip the sync while the
-- remote tree is incomplete.
invariant SourceMarkerOnlyAfterSuccess {
    for state in HostSourceStates:
        state.rolling_marker_written
            implies LastRsyncForHostReturnedOK(state.host, state.working_dir)
}

-- The per-job marker the runner reads at preflight must encode the SHA
-- the dispatcher queued the job against, not the rolling per-workdir
-- value (which a peer job's rsync may have replaced). Sibling jobs
-- sharing the same working directory each get their own marker file.
invariant PerJobMarkerCarriesQueuedSHA {
    for marker in PerJobSourceMarkers:
        marker.expected_sha = DispatchEntry{job: marker.job}.source_sha256
}

-- Source provenance marker files (both rolling and per-job) must be in
-- DefaultExcludes so that rsync --delete from a peer job's sync cannot
-- reap them. Reaping the per-job marker would manifest as a spurious
-- source_provenance_mismatch on the next preflight.
invariant SourceMarkersExcludedFromRsync {
    for pattern in {".weft-source.sha256", ".weft-source.*.sha256"}:
        exists ExcludePattern{pattern: pattern, origin: builtin}
}

-- An R2-isolated dispatch (source_r2_key set) MUST NOT rsync the working
-- dir or write a per-job marker for that dispatch; the source comes from
-- the content-addressed tarball and the SHA is implicit in the key.
invariant R2IsolatedDispatchSkipsRsync {
    for entry in DispatchEntries where entry.source_r2_key != null:
        not exists RsyncInvocation{
            host: entry.job.host,
            working_dir: entry.job.working_dir,
            for_job: entry.job
        }
        and not exists PerJobSourceMarker{job: entry.job}
}

-- Syncing to the machine weft is running on is a no-op. Rsyncing a
-- directory to itself is not merely wasteful; with --delete it can
-- permanently destroy files. The local-host short-circuit is load-bearing.
invariant LocalHostSourceSyncIsNoOp {
    for job in jobs/Jobs:
        (job.host != null and IsLocalHost(job.host))
            implies not exists RsyncInvocation{host: job.host, working_dir: job.working_dir}
}

-- Declared local_path inputs must resolve to a path inside (or beneath)
-- the job's working directory. Paths that escape working_dir via "../"
-- are rejected at classification time; they cannot safely be carried in
-- a content-addressed source snapshot whose identity is tied to working_dir.
invariant LocalInputsInsideWorkingDir {
    for job in jobs/Jobs:
        for input in job.inputs where kind = local_path:
            IsInsideWorkingDir(input.file_path, job.working_dir)
}

-- Every R2 object under the sources/ prefix is keyed by the content hash
-- of the object it stores. This lets many instances share one tarball and
-- lets the uploader skip work when the hash already exists.
invariant R2SourceObjectsContentAddressed {
    for obj in CloudSourceObjects:
        obj.r2_key = "sources/" + obj.snapshot.content_hash + ".tar.gz"
}

-- Cloud source repair must inspect every mount in the job's source-root
-- manifest. Seeing root 0 populated is not evidence that a sibling mount is
-- present.
invariant CloudSourceRepairChecksEveryMount {
	for job in jobs/Jobs where job.target_kind = cloud_rental:
		for root in LatestSourceRootManifestFor(job.working_dir).roots:
			BeforeSetupOrCommand(job):
				EnsureSourceRootFresh(job: job, root: root)
}

-- Every byte a job's command downloads from an HF-style source at runtime
-- must correspond to some declared input on the job. Undeclared downloads
-- break disk budgeting and make cloud instances susceptible to disk-full
-- termination. Enforced by post-mortem comparison of observed_inputs
-- against declared inputs (see Job.observed_inputs in job-lifecycle).
invariant DeclaredInputsCoverDownloads {
    for job in jobs/Jobs where job.is_terminal:
        for observed in job.observed_inputs:
            exists declared in job.inputs: declared = observed

    @guidance
        -- Violations are not hard errors — the job has already finished —
        -- but they surface as warnings in `weft job show` and feed into
        -- the divergence tooling so maintainers can update script
        -- metadata to declare the missing input.
}

-- On-prem stage-or-block: a validly-parsed declared input that cannot be
-- staged or satisfied on the target host must BLOCK dispatch — the job is
-- never appended to the runner queue, and a dispatch-failed event/reason
-- is recorded — rather than letting the command run and fail with
-- missing_file. This is the on-prem counterpart of the guiding principle
-- and of AgentStagingContract on rentals, where the agent refuses to start
-- a job whose declared inputs are not all resolvable. Historically each
-- input scheme was retrofitted with fail-loud behaviour independently
-- (bugs wb28, wb29); this invariant is the shared obligation any new
-- input scheme must satisfy from day one.
invariant DeclaredInputStagedOrDispatchBlocked {
    -- job.inputs are parsed InputRefs: an unrecognised prefix already
    -- aborted submission (see ClassifyInputs), so every declared input
    -- reaching dispatch is validly parsed.
    for job in jobs/Jobs where job.target_kind = inventory_host:
        for input in job.inputs:
            not InputSatisfiableOnHost(input, job.host)
                implies (not exists RunnerQueueAppend{job: job}
                         and exists DispatchFailureRecorded{job: job})

    @guidance
        -- Per-scheme enforcement points (internal/ops/host_sync.go unless
        -- noted):
        --   hf: / hf-dataset:   — ensureHFInputsAvailable builds and runs
        --     the prestage plan; on failure recordFailure emits
        --     EventQueueDispatchFailed("input staging failed") and the
        --     dispatch loop `continue`s past the queue append.
        --   asset: (named_asset) — mirrored into --needs at submission
        --     (cmd/run.go); stageArtifactNeedsForHost stages from R2; a
        --     stage failure puts the job in stageFailures and the dispatch
        --     loop skips it before the queue append.
        --   checkpoint: / corpus: — satisfied by construction: the
        --     placement hard filter (NonTransportableInputsPinPlacement;
        --     internal/placement/placement.go
        --     missingNonTransportableInputReason) never places the job on
        --     a host that lacks the path, so dispatch only ever sees
        --     satisfiable inputs.
        -- Host-unreachable is a deferral (EventQueueDispatchDeferred),
        -- not a violation: the input may be satisfiable once the host is
        -- back.
}

-- Disk estimation for cloud placement must account for every disk
-- consumer weft itself knows about before launch or reuse admission:
--   * declared HF inputs (deduplicated across the instance's jobs),
--   * every source root that weft syncs onto the instance,
--   * the project environment (uv sync footprint from the manifest or
--     lockfile),
--   * isolated PEP-723 script environments — heavy ML frameworks declared
--     via inline `dependencies` or `uv run --with` materialize their own
--     env under environments-v2, which on provider filesystems can be a
--     wheel clone/copy rather than a free hardlink (the wb39 fix).
-- Undercounting any of these risks disk-full termination mid-run on a
-- correctly-declared job.
invariant DiskEstimateCoversDeclaredFootprint {
    for group in InstanceGroups:
        EstimatedDiskGB(group) >=
            DeclaredHFInputGB(group)
			+ SourceRootGB(group)
            + ProjectEnvGB(group)
            + IsolatedScriptEnvGB(group)

    @guidance
        -- Implementation anchor (internal/campaign/):
        --   disk.go EstimateGroupDisk         — fresh-launch sizing
		--   disk.go estimateGroupSourceRootBytes — project and sibling
		--     source-root bytes
        --   disk.go heavyDepIncrementalEnvGB,
        --           commandDepIncrementalEnvGB — isolated PEP-723 env /
        --     wheel-clone overhead per heavy framework (torch, vllm,
        --     sglang, tensorflow, jax/jaxlib, lmcache)
        --   reuse.go estimateReuseJobDiskNeedGB — reuse admission charges
        --     the incremental script-env estimate against current free
        --     disk (scriptEnvGB term).
        -- Related: DeclaredInputsCoverDownloads keeps the declared-input
        -- term honest; an undeclared download escapes this estimate
        -- entirely.
}

-- ==========================================================================
-- Surfaces
-- ==========================================================================

-- The boundary between Weft's local sync/launch path and the cloud agent
-- (running on a rental instance). Weft fulfils the
-- staging contract; the agent demands it by refusing to start a job
-- whose declared inputs are not all resolvable.
surface AgentStagingInterface {
    facing agent: cloud/Instance

    context instance: cloud/Instance

    exposes:
        instance.campaign_manifest
        instance.source_hash

    contracts:
        fulfils AgentStagingContract

    @guarantee ManifestPublishedAfterAllInputsStaged
        -- weft does not publish the campaign manifest for an instance
        -- until (a) the sources/<hash> object exists in R2, and (b)
        -- every needs/<key> referenced by any job in the manifest exists
        -- in R2. The agent may treat a published manifest as proof that
        -- all declared inputs are resolvable.

    @guidance
        -- Implementation anchor:
        --   internal/cloud/wrapper.go       — manifest publication + grace wrapper
        --   internal/campaign/launch.go     — orchestrates upload → manifest
        --   cmd/agent/jobloop.go            — agent side of the contract
}

-- ==========================================================================
-- Open Questions
-- ==========================================================================

open question "HF cloud prestaging venue: should HF assets be staged via R2
(weft uploads, agent pulls) or via direct transfer from a donor host into
the instance at bootstrap? The current absence of either path is a bug;
which one to implement is not yet settled. R2 costs more but unifies the
staging story; donor-host transfer is cheaper but requires the donor to
be reachable from the instance."

open question "Source snapshot reuse across working directories: if two
sibling project checkouts produce the same content_hash, should they share
a HostSourceState row, or are host sources directories keyed strictly by
working_dir? Current rules assume the latter; cross-working-dir sharing
would save rsync time but complicates --delete semantics."

open question "Extra-path sync with --delete: SyncExtraFilePaths currently
does not pass --delete, so files deleted locally linger on the remote.
Acceptable for HF caches (the user wouldn't use local_path for those) but
surprising for general data directories. Should extras gain an opt-in
--delete or a per-path policy?"
